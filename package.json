{
  "name": "grkk-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "backend": "cd backend && .venv\\Scripts\\python app.py"
  },
  "devDependencies": {
    "cypress": "^15.17.0"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
